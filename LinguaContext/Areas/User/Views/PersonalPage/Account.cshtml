@model User

@{
    ViewData["Title"] = "Мой аккаунт";
}

<div class="card mt-4 p-2" style="width:50%; left:25%">
    <div class="row">
        <div class="col-6">
            <p class="h2">@Model.Name</p>
            <p class="h5 text-secondary">@Model.UserName</p>
        </div>
        <div class="col-6">
            <img src="@Model.AvatarUrl" class="card-img-top rounded" height="100%" />
        </div>
    </div>
    <hr/>
</div>